name: hello_world

# trigger
on:
- push

jobs:
  greetings:
    runs-on: ubuntu-latest
    steps:
    - id: s00
    # eunning shell(bash/zsh etc) commd 
      run: echo "current time in Github $(date)"
    - id: s01
      run: ls -al
    - id: s02
      #cat /new1/notes.txt
      uses: mscoutermarsh/cowsays-action@v1
      with:
        text: 'hi'
        color: 'red'
